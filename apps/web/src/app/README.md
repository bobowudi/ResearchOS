# Web App Structure

Target structure for the Vue app:

- `src/app`: application shell, router, providers, layouts
- `src/features`: feature-owned pages, components, composables, clients
- `src/shared`: cross-feature UI and utilities

Current MVP code is still being migrated into this structure.
